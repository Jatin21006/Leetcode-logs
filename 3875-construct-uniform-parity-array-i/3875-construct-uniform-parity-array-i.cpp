class Solution {
public:
    bool uniformArray(vector<int>& nums1) {
        //koi ek bhi odd hai toh odd bana sakte hai pura array
        //so we can make uniform parity in every case
        return true;
        
    }
};